// Simplistic page to re-export or just proxy WorkerDashboard for the job feed requirement
import WorkerDashboard from './WorkerDashboard';

const JobFeed = () => {
  return <WorkerDashboard />;
};

export default JobFeed;
